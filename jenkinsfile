pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                git branch: 'main', url: 'https://github.com/saibalban2/my-portfolio.git'
            }
        }
     
    }
}
